apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: astro-pvc
  namespace: astro
spec:
  storageClassName: "local-path"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.storage.size }}
