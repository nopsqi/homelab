apiVersion: v1
kind: Service
metadata:
  name: linux-box-service
  labels:
    app: linux-box
spec:
  selector:
    app: linux-box
  ports:
  - protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
