apiVersion: v1
kind: Service
metadata:
  name: naila-postgres
  labels:
    app: naila-postgres
spec:
  selector:
    app: naila-postgres
  ports:
  - protocol: TCP
    port: {{ .Values.postgres.service.port }}
    targetPort: {{ .Values.postgres.service.port }}
