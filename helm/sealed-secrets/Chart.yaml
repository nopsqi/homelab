apiVersion: v2
name: sealed-secrets
description: Bitnami Sealed Secrets controller
version: 0.1.0
dependencies:
  - name: sealed-secrets
    version: "2.18.3"
    repository: "https://bitnami-labs.github.io/sealed-secrets"
